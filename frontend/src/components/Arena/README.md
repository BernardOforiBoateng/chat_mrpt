# Arena Components

LLM arena mode components for model comparison.

## Components

- **DualResponsePanel.tsx** - Side-by-side LLM responses
- **VotingButtons.tsx** - User voting interface

## Purpose

Enables users to compare different LLM responses side-by-side and vote on which response is better for continuous model evaluation.
