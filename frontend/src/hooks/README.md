# Custom Hooks

Reusable React hooks for common functionality.

## Key Hooks

- **useMessageStreaming.ts** - Server-sent events (SSE) streaming hook
- **useVisualization.ts** - Visualization state and rendering logic

## Purpose

Encapsulates complex stateful logic and side effects into reusable hooks following React best practices.
